---
name: gareth-os-optimizer
description: Audit and improve Gareth's Second Brain instructions, knowledge links, evidence freshness and navigation. Use for OS optimizer, workspace audit, skill cleanup or architecture review. Preserve useful task knowledge and evaluate changes against actual task behavior.
---

# Gareth OS Optimizer

Resolve the user's workspace and current authority before making changes. Use [the portable audit passes](references/portable-audit.md). Read only the relevant passes for a targeted review; a requested full audit covers all nine and reports skipped checks explicitly.

Inventory files without following symbolic links or Windows reparse points. Respect ignore and privacy rules. Keep generated inventories and recovery copies outside the scan root. Discover actual roles and navigation, including the five engines and separate Life areas where configured. Do not impose a folder taxonomy or create a root configuration file just to satisfy a framework.

Each finding needs a source location, observed consequence, concrete proposed change, permission scope and verification method. Apply the authorized reversible changes after preparing recoverable originals. Ask only when a material choice or action lacks authority; existing authorization need not be requested again. Keep uncertain findings unresolved with a reason.

Use current official host documentation for loading, discovery, permissions and limits. Model behavior requires a relevant task evaluation. File sizes, token estimates, overlap metrics and age can select candidates; they do not establish failure, factual staleness or safety.

Do not delete skills because a model is more capable. Retain useful domain knowledge, adapt conflicting or obsolete instructions, and retire redundant entrypoints only after unique functions and recovery are verified. No physical deletion is implied by an audit. Preserve raw evidence and private local variants.

Report findings in a saved Markdown report and machine-readable sidecar when useful. Preserve the existing HTML dashboard option using the report templates if requested; escape source text, validate substitutions and visually check the result. Use verified counts and coverage rather than an invented health score or annual token savings. Show a concise summary and report link in chat.

The older nine pass files, framework essays and HTML templates remain recoverable historical resources. The active procedure is portable-audit.md; older thresholds, auto-delete paths, fixed four-engine routing, Claude tool names and conflicting dash rules are not governing instructions. Read historical resources only to trace a specific check's provenance.
